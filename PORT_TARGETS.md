# Port targets — `derive-more-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `derive_more` that codex-rs uses, and therefore the minimum subset that `derive-more-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-41] (rank `272`, tier `direct`, status `To Do`)

**Cargo package name**: `derive_more`
**Rust module name (in `use` statements)**: `derive_more`

## Summary

- **2** distinct symbols imported by codex-rs
- **4** total `use` statements
- **4** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `derive_more::derive::Display`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-execpolicy-legacy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `execpolicy-legacy/src/arg_matcher.rs:6` (`codex-execpolicy-legacy`) — `use derive_more::derive::Display`
- `execpolicy-legacy/src/arg_type.rs:7` (`codex-execpolicy-legacy`) — `use derive_more::derive::Display`
- `execpolicy-legacy/src/opt.rs:6` (`codex-execpolicy-legacy`) — `use derive_more::derive::Display`

### `derive_more::IsVariant`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/custom_terminal.rs:37` (`codex-tui`) — `use derive_more::IsVariant`
