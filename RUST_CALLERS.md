# Rust callers — `derive-more-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `derive_more` | `derive_more` | 23 | 5 | 3 | 4 |

## `derive_more`

### Inbound symbols (4 distinct, 23 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `derive_more`.

#### `derive_more::derive::Display` — 3 site(s) (3 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-execpolicy-legacy` `src/arg_matcher.rs:6` — `use derive_more::derive::Display`
- ✅ `codex-kotlin/codex-execpolicy-legacy` `src/arg_type.rs:7` — `use derive_more::derive::Display`
- ✅ `codex-kotlin/codex-execpolicy-legacy` `src/opt.rs:6` — `use derive_more::derive::Display`

#### `derive_more::IsVariant` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/custom_terminal.rs:37` — `use derive_more::IsVariant`

#### `derive_more::Display` — 18 site(s) (0 codex, 18 other, 2 caller crates)

- `starlark-kotlin/starlark` `src/docs/tests/markdown.rs:4` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/docs/tests/rustdocs.rs:4` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/environment/methods.rs:273` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/eval/bc/addr.rs:10` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/eval/compiler/def.rs:13` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/pagable/vtable_registry.rs:89` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/stdlib.rs:146` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/tests/derive/attrs.rs:4` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/tests/derive/docs.rs:4` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/tests/opt/if_rand.rs:7` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/tests/runtime.rs:9` — `use derive_more::Display`
- `starlark-kotlin/starlark` `src/tests/uncategorized.rs:8` — `use derive_more::Display`
- _… and 6 more site(s)._

#### `derive_more::derive::IsVariant` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `crossterm-kotlin/crossterm` `src/event.rs:131` — `use derive_more::derive::IsVariant`
